import Nav from '../../components/Nav/Nav'
import Layout from '../../layout/Layout';

const Home = () => {
    return (
        <Layout>
            <div>Home</div>
        </Layout>
    );
}

export default Home;